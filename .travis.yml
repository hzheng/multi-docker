sudo: required
language: generic

services:
  - docker

before_install:
  - docker build -t pie01/react-test -f ./client/Dockerfile.dev ./client

script:
  - docker run -e CI=true pie01/react-test npm test

after_success:
  - docker build -t pie01/multi-client ./client
  - docker build -t pie01/multi-nginx ./nginx
  - docker build -t pie01/multi-server ./server
  - docker build -t pie01/multi-worker ./worker
  # Log in to the docker CLI
  - echo "$DOCKER_PASSWORD" | docker login -u "$DOCKER_ID" --password-stdin
  # Take those images and push them to docker hub
  - docker push pie01/multi-client
  - docker push pie01/multi-nginx
  - docker push pie01/multi-server
  - docker push pie01/multi-worker
